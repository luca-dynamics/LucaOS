# Luca Tier Routing Preview Adapter

`src/types/lucaTierRoutingPreview.ts` provides pure metadata previews from `resolveLucaTierShellMode`.

It does not import React or call services and keeps `runtimeBehaviorChanged` and `uiWiringChanged` hard-false.
