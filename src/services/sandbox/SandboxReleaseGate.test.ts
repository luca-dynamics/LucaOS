import { describe, expect, it } from "vitest"; import { SandboxReleaseGate } from "./SandboxReleaseGate";
describe("SandboxReleaseGate", () => { it("blocks shipping without scanner and signed hardware evidence", async () => { const gate = new SandboxReleaseGate(new Uint8Array(32)); const decision = await gate.evaluate({ kind: "docker", platform: "windows", installedAssets: [], availableFeatures: ["docker-daemon"], scannerAvailable: false }); expect(decision.status).toBe("blocked"); expect(decision.blockers).toEqual(expect.arrayContaining(["Artifact scanner is unavailable.", "Signed hardware certification is missing."])); expect(decision.hostFallbackAllowed).toBe(false); }); it("never promotes preview backends through configuration alone", async () => { const gate = new SandboxReleaseGate(new Uint8Array(32)); const decision = await gate.evaluate({ kind: "firecracker", platform: "linux", installedAssets: ["firecracker-kernel", "firecracker-rootfs"], availableFeatures: ["kvm", "firecracker"], scannerAvailable: true }); expect(decision.tier).toBe("preview"); expect(decision.status).toBe("blocked"); }); });
