# Browser Runtime Router

`BrowserRuntimeRouter` is a lightweight routing scaffold for browser automation requests.

## Purpose

- Accept normalized `BrowserRuntimeRequest` objects.
- Select the first registered adapter that can handle a request.
- Return a consistent `BrowserRuntimeRouteResult` response.

## Current status

This service is a scaffold intended for integration with mission orchestration and policy checks in later tasks.
