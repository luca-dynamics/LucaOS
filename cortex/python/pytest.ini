[pytest]
# Cortex backend test harness. Run from cortex/python:
#   venv/Scripts/python -m pytest      (Windows)
#   venv/bin/python -m pytest          (macOS/Linux)
testpaths = tests
pythonpath = .
addopts = -q
