const test = require('node:test'); const assert = require('node:assert/strict'); const fs = require('fs'); const path = require('path');
test('certification requires every real-host assertion and a signing key', () => { const source = fs.readFileSync(path.resolve(__dirname, '..', 'certify-sandbox-backend.cjs'), 'utf8'); for (const check of ['probeIsolated', 'createDestroy', 'commandIsolation', 'networkPolicy', 'artifactIntegrity', 'snapshotCleanup', 'crashRecovery']) assert.match(source, new RegExp(check)); assert.match(source, /CERTIFICATION_PRIVATE_KEY/); assert.match(source, /crypto\.sign/); });
